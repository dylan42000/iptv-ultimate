# DYLANDOS IPTV ULTIMATE — release signing configuration
#
# Copy this file to `keystore.properties` (in the `android/` directory) and fill
# in your real values, OR just run scripts/build-signed-apk.ps1 / .sh which writes
# it automatically and generates the keystore for you.
#
# WARNING: keystore.properties is git-ignored. Do not commit real passwords.

storeFile=app/dylandos-release.jks
keyAlias=dylandos
storePassword=dylandos2026
keyPassword=dylandos2026
